import 'package:flutter/material.dart';

class NewFeed extends StatelessWidget {
  const NewFeed ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text(' New Feed'),),

      
    );
  }
}